;; -*- mode: Lisp; -*-

(setq prj-version nil)

(setq prj-list
      '(("eproject" "/home/oracleyue/.emacs.d/git/emacs-for-python/extensions/eproject")))

(setq prj-last-open nil)

(setq prj-frame-pos
      '((top . 225)
        (left . 1029)
        (width . 90)
        (height . 34)))

